# 开发环境：前端由 Vite dev server 提供（默认 5173，见 Security & RBAC Detail V1.0 §81）
# /api 与 /actuator 通过 vite.config.ts 的 dev proxy 转发到 Spring Boot
VITE_API_BASE_URL=/api/v1
VITE_DEV_SERVER_PORT=5173
VITE_BACKEND_TARGET=http://localhost:8080
